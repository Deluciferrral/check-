{
  "name": "check-cashing-website",
  "version": "1.0.0",
  "description": "A Netlify website to cash checks with Stripe integration",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Build complete'"
  },
  "dependencies": {
    "@stripe/stripe-js": "^2.2.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0",
    "stripe": "^14.0.0"
  }
}
